# saveweb2zip-com-ibz-moyodev-netlify-app

Simple static website project saved from a site. Contains HTML, CSS, fonts, images, and JS.

## Quick start

```bash
cd /Users/apple/Downloads/saveweb2zip-com-ibz-moyodev-netlify-app
# initialize local repo (if not already done)
git init
# add files and commit
git add .
git commit -m "Initial commit"

# create a GitHub repository and push (choose one method)
# 1) Using GitHub CLI (recommended if installed):
#    gh repo create <owner>/<repo> --public --source=. --remote=origin --push

# 2) Manual create on github.com, then:
#    git remote add origin https://github.com/<owner>/<repo>.git
#    git branch -M main
#    git push -u origin main
```

## Notes
- `.gitignore` added to exclude common OS, editor, and node artifacts.
- Consider adding a `LICENSE` if you plan to publish.
